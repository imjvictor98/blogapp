<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <p>Descricao: {{postagem.descricao}}</p>
        <p><bold>Data de publicacao: {{#formatDate postagem.data}}{{/formatDate}}</bold></p>
        <hr>
        {{postagem.conteudo}}
    </div>
</div>
